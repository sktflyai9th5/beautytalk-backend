# 모델 없이 라우팅·전처리만 확인할 때 (GPU 불필요, CPU 로 뜬다)
#   docker compose -f docker-compose.yml -f docker-compose.mock.yml up -d --build
services:
  backend:
    environment:
      - BT_MOCK=1
    deploy:
      resources:
        reservations:
          devices: []
